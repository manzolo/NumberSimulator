{
  "name": "edu-num-playground",
  "version": "1.0.0",
  "description": "EDU-NUM — an interactive number-representation simulator: a hand-written engine converts, adds, negates and encodes numbers and animates every bit, step by step — from binary and hex to two's complement, IEEE-754 floating point and character encodings. Guided levels and a free sandbox. Zero dependencies, zero build.",
  "type": "module",
  "private": true,
  "homepage": "https://manzolo.github.io/NumberSimulator/",
  "repository": {
    "type": "git",
    "url": "https://github.com/manzolo/NumberSimulator.git"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "e2e": "node tools/e2e.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "license": "MIT"
}
